#include "main.h"

int _strcspsn(char *source, char s)
{
        int i, result = 0;

        for (i = 0; source[i] != '\0'; i++)
        {
		result++;
                if (*source == s)
                        break;
        }

        return (result);
}
